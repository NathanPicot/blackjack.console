# web_application1

WebApplication1 - JavaScript client for web_application1
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0
- Package version: 1.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install web_application1 --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your web_application1 from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var WebApplication1 = require('web_application1');


var api = new WebApplication1.CarteApi()
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully.');
  }
};
api.apiCarteGetAllGet(callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*WebApplication1.CarteApi* | [**apiCarteGetAllGet**](docs/CarteApi.md#apiCarteGetAllGet) | **GET** /api/Carte/getAll | 
*WebApplication1.CarteApi* | [**apiCarteGetIdGet**](docs/CarteApi.md#apiCarteGetIdGet) | **GET** /api/Carte/get {id} | 
*WebApplication1.JoueurApi* | [**apiJoueurAddPost**](docs/JoueurApi.md#apiJoueurAddPost) | **POST** /api/Joueur/add | 
*WebApplication1.JoueurApi* | [**apiJoueurDeleteIdDelete**](docs/JoueurApi.md#apiJoueurDeleteIdDelete) | **DELETE** /api/Joueur/delete {id} | 
*WebApplication1.JoueurApi* | [**apiJoueurGetAllGet**](docs/JoueurApi.md#apiJoueurGetAllGet) | **GET** /api/Joueur/getAll | 
*WebApplication1.JoueurApi* | [**apiJoueurGetIdGet**](docs/JoueurApi.md#apiJoueurGetIdGet) | **GET** /api/Joueur/get {id} | 
*WebApplication1.JoueurApi* | [**apiJoueurUpdateIdPut**](docs/JoueurApi.md#apiJoueurUpdateIdPut) | **PUT** /api/Joueur/update {id} | 
*WebApplication1.PartieApi* | [**apiPartieAddPost**](docs/PartieApi.md#apiPartieAddPost) | **POST** /api/Partie/add | 
*WebApplication1.PartieApi* | [**apiPartieDeleteIdDelete**](docs/PartieApi.md#apiPartieDeleteIdDelete) | **DELETE** /api/Partie/delete {id} | 
*WebApplication1.PartieApi* | [**apiPartieGetAllGet**](docs/PartieApi.md#apiPartieGetAllGet) | **GET** /api/Partie/getAll | 
*WebApplication1.PartieApi* | [**apiPartieGetIdGet**](docs/PartieApi.md#apiPartieGetIdGet) | **GET** /api/Partie/get {id} | 
*WebApplication1.PartieApi* | [**apiPartieUpdateIdPut**](docs/PartieApi.md#apiPartieUpdateIdPut) | **PUT** /api/Partie/update {id} | 


## Documentation for Models

 - [WebApplication1.Joueur](docs/Joueur.md)
 - [WebApplication1.Partie](docs/Partie.md)


## Documentation for Authorization

Endpoints do not require authorization.

